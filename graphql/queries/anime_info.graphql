query AnimeInfo($id: Int!) {
  Media(type: ANIME, id: $id) {
    title {
      romaji
      native
    }
    format
    description
    season
    seasonYear
    episodes
    averageScore
    status
    siteUrl
    genres
    coverImage {
      extraLarge
    }
  }
}
